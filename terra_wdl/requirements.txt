# Python dependencies baked into the Pegasus Docker image.
# Installed during `docker build` via Dockerfile.pegasus.
#
# Strategy: pin pegasuspy and packages our code directly imports;
# let pip resolve the pegasus ecosystem (igraph, leidenalg, anndata, etc.)
# to avoid version conflicts within that tightly-coupled dependency tree.

# Build toolchain – must be pinned explicitly so it survives full pip dependency
# resolution. pkg_resources (part of setuptools) is imported by pegasus at runtime.
setuptools>=65.0

# Core single-cell analysis
# NOTE: PyPI package name is pegasuspy (not pegasus); imports as `import pegasus`
pegasuspy==1.9.1.post1
scrublet==0.2.3
doubletdetection==4.2

# Harmony batch correction (harmonypy API used directly in Pegasus-Pipeline.py)
harmonypy==0.0.9

# Plotting (pinned — used directly in Pegasus-Pipeline.py)
matplotlib==3.7.5
seaborn==0.13.2
