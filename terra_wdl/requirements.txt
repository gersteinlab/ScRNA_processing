# Python dependencies baked into the Pegasus Docker image.
# Installed during `docker build` via Dockerfile.pegasus.
#
# Strategy: pin pegasuspy and packages our code directly imports;
# let pip resolve the pegasus ecosystem (igraph, leidenalg, anndata, etc.)
# to avoid version conflicts within that tightly-coupled dependency tree.
#
# NOTE: setuptools (pkg_resources) is handled directly in the Dockerfile,
# NOT here — see the "Hard fix for pkg_resources" block in Dockerfile.pegasus.

# Core single-cell analysis
# NOTE: PyPI package name is pegasuspy (not pegasus); imports as `import pegasus`
pegasuspy==1.9.1.post1
scrublet==0.2.3
doubletdetection==4.2

# Harmony batch correction — pegasus imports 'harmony' (from harmony-pytorch),
# NOT 'harmonypy'. The two are completely different packages.
harmony-pytorch

# HDF5 file I/O — used directly in Pegasus-Pipeline.py to patch NeMo h5 files
# (feature_type/genome fields are 'NA' from STARsolo/Optimus; must be patched to
#  'Gene Expression'/'GRCh38' so pegasusio assigns modality='rna' correctly)
h5py>=3.7.0

# Plotting (pinned — used directly in Pegasus-Pipeline.py)
matplotlib==3.7.5
seaborn==0.13.2
